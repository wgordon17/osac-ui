name: Container build

on:
  pull_request:

jobs:
  build:
    name: Build container image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build image
        run: podman build -t osac:pr-${{ github.event.pull_request.number }} -f Containerfile .
